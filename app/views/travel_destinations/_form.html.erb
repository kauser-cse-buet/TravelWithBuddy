<%= form_for(travel_destination) do |f| %>
  <% if travel_destination.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(travel_destination.errors.count, "error") %> prohibited this travel_destination from being saved:</h2>

      <ul>
      <% travel_destination.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
