<p id="notice"><%= notice %></p>

<h1>Invitations</h1>

<table>
  <thead>
    <tr>
      <th>From</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @invitations.each do |invitation| %>
      <tr>
        <td><%= invitation.from %></td>
        <td><%= invitation.description %></td>
        <td><%= link_to 'Join', new_invitations_path(:id => travel_event.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

